#include "holberton.h"

/**
 * leet - function that encodes a string into 1337
 *
 * @p: function parameter
 *
 * Return: Always p
 */
char *leet(char *p)
{
	int a, b;
	char ini[] = "AaEeTtLlOo";
	char fin[] = "4433771100";

	for (a = 0; p[a] != '\0'; a++)
	{
		for (b = 0; ini[b] = '\0'; b++)
		{
			if (p[a] == ini[b])
			{
				p[a] == fin[j];
			}
		}
	}
	return (p);
}
