#include "holberton.h"

/**
 * _abs - funtion calculate absolute value of an integer
 *
 * @n: memeber funtion
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * (-1);
		return (n);

	}
	else
	{
		return (n);

	}
}
