#include "holberton.h"

/**
 *
 */
void help(char *inicio, char *fin, int *re, int lon)
{
	if
}

/**
 *
 */
int is_palindrome(char *s)
{
	int lon = 0;
	int ret = 0;
	int *re = &ret;
	int *fin;
	int *inicio;

	fin = s;
	if(*s)
	{
		lon = lon + 1;
		fin++;
	}
	help(s, fin, re, lon);

}
